// Close any modals or overlays
$("#overlay").hide();
$("#modal").hide();

// Optionally, you can update the schedule list or display a success message
// Example:
// $("#schedule-list").append("<%= j render(partial: 'schedule', locals: { schedule: @schedule }) %>");

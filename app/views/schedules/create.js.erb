// create.js.erb

console.log("create.js.erb is being executed");

<% if @schedule.save %>
  // Redirect if needed
  window.location.href = "<%= schedules_path %>";

  // Display success message for a specific duration
  setTimeout(function() {
    document.querySelector('.alert').style.display = 'none';
  }, 5000); // Hide after 5 seconds
<% else %>
  // Handle other cases if needed
<% end %>


